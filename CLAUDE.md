# SKALA-SKCT Convention

이 문서는 SKALA-SKCT organization의 모든 레포에서 공통으로 따르는 작업 규칙입니다.  
레포별 세부 규칙이 필요한 경우 각 레포의 README 또는 별도 문서에서 보완합니다.

## 기본 원칙

- 모든 작업은 이슈 생성에서 시작합니다.
- 코드 변경은 이슈가 연결된 브랜치에서 진행하고 PR로 병합합니다.
- 사용자가 실제로 보는 화면과 흐름을 기준으로 판단합니다.
- 기존 코드 스타일과 폴더 구조를 먼저 따릅니다.
- 불필요한 리팩터링, 파일 이동, 포맷 변경은 피합니다.
- 하나의 변경은 하나의 목적만 갖도록 작게 유지합니다.
- 배포 URL, API URL, 외부 서비스 URL은 가능한 환경변수로 관리합니다.
- 인증, 세션, 개인정보, 시험 결과 데이터는 보수적으로 다룹니다.

## 코드 스타일

- TypeScript를 기본으로 사용합니다.
- 컴포넌트와 함수 이름은 역할이 드러나게 작성합니다.
- UI 컴포넌트는 재사용 가능성이 명확할 때만 분리합니다.
- 조건문이 복잡해지면 의미 있는 변수나 작은 함수로 나눕니다.
- 매직 넘버와 반복 문자열은 상수로 분리합니다.
- 주석은 코드만으로 의도가 드러나지 않는 경우에만 짧게 작성합니다.
- 사용하지 않는 코드, 주석 처리된 코드, 임시 로그는 커밋하지 않습니다.

## 폴더/파일 규칙

- 페이지 라우트는 Next.js App Router 구조를 따릅니다.
- 공통 UI는 `components` 아래에 둡니다.
- 서비스 설정, 상수, 유틸리티는 `lib` 아래에 둡니다.
- DB 스키마와 데이터 접근 코드는 기존 `db` 구조를 따릅니다.
- 새 폴더를 만들 때는 해당 영역의 책임이 분명해야 합니다.

## UI/UX 규칙

- 랜딩페이지는 서비스 진입과 하위 페이지 이동에 집중합니다.
- 하위 학습 페이지는 각자의 핵심 기능을 첫 화면에서 바로 사용할 수 있어야 합니다.
- 버튼, 입력창, 링크의 문구는 사용자의 다음 행동이 명확하게 보이도록 작성합니다.
- 오류 메시지는 원인과 다음 행동을 짧게 안내합니다.
- 불필요한 안내 문구나 개발 중 문구는 운영 화면에 노출하지 않습니다.
- 모바일/데스크탑에서 텍스트가 잘리거나 겹치지 않도록 확인합니다.

## 인증/도메인 규칙

- 로그인과 계정 관련 화면은 Mother 레포에서 관리합니다.
- 하위 학습 페이지는 자체 로그인/회원가입 화면을 만들지 않습니다.
- 로그인되지 않은 사용자는 Mother의 로그인 화면으로 이동시킵니다.
- 배포 URL은 각 레포의 환경변수와 배포 설정을 기준으로 관리합니다.
- 추후 `skala-skct.com` 연결 시 하위 페이지는 서브도메인 방식을 우선 고려합니다.
- 도메인 값은 하드코딩보다 환경변수를 우선합니다.

## 커밋 컨벤션

커밋 메시지는 Conventional Commits 형식을 사용합니다.

```text
type: summary
```

사용하는 타입:

- `feat`: 새로운 기능
- `fix`: 버그 수정
- `refactor`: 동작 변경 없는 구조 개선
- `style`: UI 스타일 또는 포맷 수정
- `docs`: 문서 수정
- `test`: 테스트 추가 또는 수정
- `chore`: 설정, 의존성, 빌드 등 기타 작업
- `perf`: 성능 개선
- `revert`: 이전 커밋 되돌리기

예시:

```text
feat: add mock test dashboard
fix: redirect unauthenticated users to landing login
docs: update repository role
refactor: remove local auth pages
```

커밋 규칙:

- 제목은 72자 이내로 작성합니다.
- 제목 끝에 마침표를 붙이지 않습니다.
- 한 커밋에는 하나의 목적만 담습니다.
- 자동 생성 파일이나 빌드 산출물은 필요한 경우에만 포함합니다.

## 브랜치 컨벤션

브랜치는 반드시 연결된 이슈 번호를 포함합니다.

```text
type/#issue-number-short-description
```

예시:

```text
feat/#12-mock-dashboard
fix/#18-auth-redirect
docs/#21-readme-update
refactor/#24-remove-local-auth
```

## 이슈 컨벤션

작업 전에는 반드시 이슈를 먼저 생성합니다.  
이슈 없이 바로 커밋하거나 PR을 열지 않습니다.

이슈 제목 형식:

```text
[Type] 작업 요약
```

예시:

```text
[Feat] 실전 모의고사 대시보드 추가
[Fix] 비로그인 사용자 랜딩 로그인 이동 수정
[Docs] 레포 컨벤션 문서 정리
```

이슈 본문에는 다음 내용을 포함합니다.

```md
## 어떤 기능인가요?
- 작업 목적을 간단히 적습니다.

## 작업 상세 내용
- [ ] 해야 할 작업을 체크리스트로 적습니다.

## 참고 자료
- 관련 화면, 링크, 문서가 있으면 첨부합니다.
```

이슈 라벨 규칙:

- 작업 성격: `FEAT`, `FIX`, `DOCS`, `REFACTOR`, `STYLE`, `CHORE`
- 작업 영역: `FRONTEND`, `BACKEND`, `INFRA`, `DESIGN`
- 우선순위가 필요하면 `P0`, `P1`, `P2`, `P3`를 사용합니다.

이슈 자동화 규칙:

- 새 이슈가 생성되면 작성자를 담당자로 자동 지정합니다.
- 이슈 제목의 `[Feat]`, `[Fix]`, `[Docs]` 같은 prefix를 기준으로 라벨을 자동 부여합니다.
- 이슈 본문에 `area: frontend`, `area: backend`, `area: infra`, `area: design`이 있으면 영역 라벨을 자동 부여합니다.

## PR 컨벤션

PR은 반드시 연결된 이슈가 있어야 합니다.  
PR 제목에는 이슈 번호를 포함합니다.

```text
[Type/#issue-number] 작업 요약
```

예시:

```text
[Feat/#12] 실전 모의고사 대시보드 추가
[Fix/#18] 비로그인 사용자 랜딩 로그인 이동 수정
[Docs/#21] 레포 컨벤션 문서 정리
```

PR 본문에는 다음 내용을 포함합니다.

```md
## 관련 이슈
- Closes #이슈번호

## Summary
- 변경한 내용을 짧게 정리합니다.

## Test
- 실행한 확인 방법을 적습니다.
- 예: `npm run build`

## Note
- 리뷰어가 알아야 할 배포, 환경변수, 제한사항을 적습니다.
```

PR 규칙:

- PR은 이슈 번호가 포함된 브랜치에서 생성합니다.
- PR 본문에는 `Closes #이슈번호`를 적어 병합 시 이슈가 자동으로 닫히게 합니다.
- UI 변경이 있으면 가능하면 스크린샷을 첨부합니다.
- 인증, DB, 배포 설정 변경은 영향 범위를 명시합니다.
- 관련 없는 변경은 같은 PR에 섞지 않습니다.

PR 자동화 규칙:

- 새 PR이 열리면 작성자를 담당자로 자동 지정합니다.
- PR 작성자가 `pkm021118`이면 `borimong`을 리뷰어로 자동 요청합니다.
- PR 작성자가 `borimong`이면 `pkm021118`를 리뷰어로 자동 요청합니다.
- 그 외 작성자는 `CODEOWNERS` 기준 리뷰어를 요청합니다.
- PR 제목 prefix를 기준으로 라벨을 자동 부여합니다.
- PR 본문에 연결 이슈가 없으면 자동으로 안내 댓글을 남깁니다.
- PR 병합 전에는 최소 `npm run build` 성공 여부를 확인합니다.

## 배포 전 확인

배포 전 최소 확인:

```bash
npm run build
```

필요한 경우 추가 확인:

- 주요 페이지 접속
- 로그인 필요 흐름
- 외부 링크 이동
- 환경변수 적용 여부
- Vercel Production 배포 상태

## 금지 사항

- 비밀키, 토큰, 비밀번호, DB URL을 커밋하지 않습니다.
- 사용자 데이터나 실제 시험 데이터를 임의로 삭제하지 않습니다.
- 운영 URL을 임시 테스트 URL로 바꾸지 않습니다.
- 랜딩과 하위 페이지의 책임을 섞지 않습니다.
- 원인을 확인하지 않은 상태로 기존 코드를 대량 삭제하지 않습니다.
